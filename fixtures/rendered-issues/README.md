# Rendered Issue Fixtures

This directory contains rendered GitHub issue fixtures derived from structured Speckify
planning output.

## Current Scenario

### `authentication-basic`

Source bundle:

- `../speckify-bundle/authentication-basic.bundle.json`

Rendered issue files:

- `authentication-basic/iu.authentication.capture_credentials.md`
- `authentication-basic/iu.authentication.verify_credentials.md`

Each Markdown file corresponds to one `ImplementationUnit` from the structured planning
bundle and preserves the section ordering defined in
`docs/output-artifacts-and-issue-rendering.md`.
